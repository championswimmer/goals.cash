# Agent Information

This project uses **Bun** as the primary package manager and runtime. Please use `bun` commands for installing dependencies, running scripts, and building the project.

## Commands

- Install dependencies: `bun install`
- Run development server: `bun dev`
- Build the project: `bun run build`
- Run linting: `bun run lint`
