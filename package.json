{
  "name": "goals.cash",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "install:lib": "cd lib && npm install",
    "install:chartjs-sample": "cd chartjs-sample && npm install",
    "postinstall": "run-s install:*",
    "build:lib": "cd lib && npm run build",
    "build:chartjs-sample": "cd chartjs-sample && npm run build",
    "build": "run-s build:*",
    "start:chartjs-sample": "cd chartjs-sample && npm start",
    "test:lib": "cd lib && npm run test",
    "test": "run-s test:*",
    "clean:lib": "cd lib && npm run clean",
    "clean:chartjs-sample": "cd chartjs-sample && npm run clean",
    "clean": "run-s clean:*"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
